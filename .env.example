# Public site URL (no trailing slash). Used for sitemap, canonicals, Open Graph.
NEXT_PUBLIC_SITE_URL=https://stackclash.io

# Optional: LLM generation (npm run generate -- <idA> <idB>)
# AI_PROVIDER=xai
# XAI_API_KEY=
# OPENAI_API_KEY=
# AI_MODEL=
